# Semantic Search Example

This example will exercise ForgeML through public platform APIs: corpus ingestion, embedding feature generation, retrieval model evaluation, model registration, and deployment.


# Fraud Detection Example

This example will exercise ForgeML through public platform APIs: transaction dataset versioning, feature materialization, classifier training, approval, deployment, monitoring, drift detection, and retraining.

